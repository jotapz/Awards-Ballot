import AdminEditor from "@/features/admin/AdminEditor";

export const metadata = {
  title: "Admin | Awards Ballot",
  robots: { index: false, follow: false },
};

const Page = () => <AdminEditor />;

export default Page;
